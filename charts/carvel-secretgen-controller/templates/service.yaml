apiVersion: v1
kind: Service
metadata:
  name: {{ include "carvel-secretgen-controller.fullname" . }}
  labels:
    {{- include "carvel-secretgen-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "carvel-secretgen-controller.selectorLabels" . | nindent 4 }}
