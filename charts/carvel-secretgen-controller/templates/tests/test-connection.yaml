apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carvel-secretgen-controller.fullname" . }}-test-connection"
  labels:
    {{- include "carvel-secretgen-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carvel-secretgen-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
